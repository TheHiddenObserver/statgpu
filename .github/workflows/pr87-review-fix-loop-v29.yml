name: PR87 review fix batch v29

on:
  push:
    branches: [agent/maintenance-0.2.4-0.2.5]
    paths:
      - .github/workflows/pr87-review-fix-loop-v29.yml

permissions:
  contents: write

jobs:
  apply-review-fix:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/maintenance-0.2.4-0.2.5
          fetch-depth: 0
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Apply analytic-weight fixes with corrected tests
        run: python pr87_patch_v29.py
      - name: Install validation environment
        run: |
          python -m pip install --upgrade pip
          python -m pip install -e ".[validation,formula]"
      - name: Run full CPU validation
        run: |
          python -m py_compile \
            statgpu/linear_model/_glm_base.py \
            statgpu/inference/_sandwich.py \
            statgpu/glm_core/_solver_utils.py \
            dev/tests/test_maintenance_024_025.py
          python -m pytest dev/tests -q --tb=short
      - name: Commit source fixes
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git rm -f pr87_patch_v27.py pr87_patch_v28.py pr87_patch_v29.py
          git add \
            statgpu/linear_model/_glm_base.py \
            statgpu/inference/_sandwich.py \
            statgpu/glm_core/_solver_utils.py \
            dev/tests/test_maintenance_024_025.py
          git commit -m "fix: preserve analytic-weight inference contracts"
          git push origin HEAD:agent/maintenance-0.2.4-0.2.5
