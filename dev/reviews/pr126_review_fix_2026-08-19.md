# PR #126 fresh review-fix closure — 2026-08-19

Review standard: `.claude/skills/code-review.md`.

The review was restarted independently from the previously clean PR head rather
than inheriting its verdict. The current implementation source for the second
pass is `e4dcaeaf05af8b07f3859c09c49b1fe9382b1466`; subsequent commits in this
pass update tests, the physical validator, bilingual documentation, and this
review record without changing the statistical implementation.

## Findings closed in the first fresh pass

1. **CRITICAL — public covariance non-finite input fail-closed contract.**
   `clustered_covariance`, `two_way_clustered_covariance`, `hac_covariance`, and
   `driscoll_kraay_covariance` reject non-finite `X`/residual arrays before
   signed/grouped reductions. NumPy, maintained Torch CPU, and physical CuPy/Torch
   runner coverage lock this API boundary.

2. **HIGH — projection-created dynamic-range backend coverage.**
   Two-way demeaning refreshes its stability classification after the first entity
   projection. Maintained Torch CPU and physical CuPy/Torch fixtures prove the
   safe-raw to risky-post-projection transition.

3. **CRITICAL — order-dependent multi-tier two-way covariance reduction.**
   A third-magnitude nonnested covariance could become `Inf` only after preceding
   Torch covariance tests because signed grouped components were reduced inside
   BLAS before Cameron-Gelbach-Miller cancellation. The unsafe multi-tier branch
   now keeps grouped row products separate, decomposes group-debias factors into
   exact powers of two, and uses compensated estimator-level summation. Gram-safe
   multi-tier and ordinary single-tier paths remain vectorized.

4. **MEDIUM — precision fallback scope/performance.**
   Row-level compensated covariance accumulation is restricted to the pathological
   unsafe branch; Gram-safe multi-tier covariance stays on vectorized BLAS/GPU
   products.

5. **CRITICAL — fixed-effect recovery could reintroduce unstable group means.**
   Post-fit one-way maps had fallen back to raw scatter means and two-way recovery
   could miss projection-created cancellation risk. Effect recovery now reuses the
   shared stable group means on uncentered level residuals and certifies transformed
   residuals before accepting convergence. Strict cancellation-tail regressions are
   preserved.

6. **CRITICAL — two-way effect normalization could overflow before cancellation.**
   Repeated `time_effect * group_count` normalization could overflow even when the
   observation-weighted shift was finite. The normal-scale path defers the common
   shift until convergence and computes it with `stable_mean(time_effects[t_idx])`.

7. **CRITICAL — two-way common Gram scaling could erase a recoverable product.**
   A grouped component can survive common score scaling while a mathematically
   nonzero self/cross product underflows before inclusion-exclusion. A 2x2
   nonnested `[-1e308, 1e-100, 1e308, 1e-100]` fixture has a representable low-order
   CGM remainder. The unsupported common-Gram product range now fails closed with
   `FloatingPointError` rather than silently publishing zero.

8. **CRITICAL — centered fixed-effect maps could overflow although predictions were
   finite.**
   Fixed-effect maps are stored directly on the level-residual scale. `_grand_mean`
   remains for the diagnostic contract but `PanelOLS.predict()` no longer constructs
   an unrepresentable centered effect. NumPy/Torch and physical CuPy/Torch coverage
   include the 1-vs-10 `+/-1e308` case.

9. **MEDIUM — obsolete fixed-effect prediction mask.**
   Once level-scale maps removed grand-mean restoration, the extra host-side known-
   label mask became dead O(n) work and was removed.

10. **CRITICAL — pooling-F regressor centering could lose cancellation tails.**
    The pooled-null `X` column means used an overflow-only reduction while the rest
    of panel diagnostics had moved to the magnitude-tiered mean. The helper now
    reuses the shared stable mean. `[1e308, 1, -1e308, 0, 0, 0]` retains the exact
    `1/6` column mean on NumPy and maintained Torch CPU, with the same physical
    CuPy/Torch audit.

11. **CRITICAL — sparse two-way incidence graphs could choose an out-of-range FE
    gauge.**
    Additive entity/time effects have a common-shift null direction. The historical
    weighted-zero gauge can drive an individual compact effect beyond DBL_MAX even
    when the observed levels and an equivalent decomposition are finite. Near the
    float64 boundary only, recovery now range-centers `[entity, -time]` at its
    midrange during ALS, minimizing the stored infinity norm; ordinary-scale
    recovery keeps the historical weighted-zero gauge. A connected sparse graph at
    `5e307` scale passes direct NumPy/Torch recovery and public `fit -> predict`,
    while the existing `2**50 +/- 1` low-order regression remains unchanged.

12. **CRITICAL — delayed covariance scale restoration had a range-unsafe
    multiplication association.**
    `(covariance * large) * small` can transiently overflow although the final
    product is finite; always multiplying the small factor first has the symmetric
    underflow failure. Restoration now composes covariance, projection, and design
    factors in mantissa/exponent form, renormalizes the combined mantissa, then
    restores the final exponent in bounded power-of-two chunks. Complementing
    `1e200`/`1e-200` coordinate scales and cross-stage `1e200`/`1e-100` scales are
    covered on NumPy, maintained Torch CPU, and the physical CuPy/Torch runner.

13. **CRITICAL — stable means did not make physical `value - mean` centering safe.**
    Pooling F, constant-only classical F, parameter R-squared, and adjusted
    R-squared switch to a common dimensionless response working scale only when
    physical centering would overflow; ordinary-scale subtraction is untouched.

14. **MEDIUM — stale physical-artifact documentation.**
    Existing P100 artifacts are explicitly historical reference evidence and are
    not accepted as exact-head physical validation.

## Findings closed in the second independent pass

15. **CRITICAL — automatic PooledOLS/BetweenOLS intercepts could lose a response
    cancellation tail inside the SVD BLAS projection.**
    Both estimators construct an exact constant column and then use the shared SVD
    least-squares path. For `X=[-1,0,1]` and
    `y=[2**55, 1, -2**55]`, the historical `weighted_u_t @ y` reduction could
    return an intercept of zero although the correct intercept is `1/3`. The
    cancellation-sensitive path now preserves the same SVD factors, rank cutoff,
    design scaling, and minimum-norm parameterization but reduces the explicit
    response-projection products with the shared magnitude-tiered grouped reducer.
    Ordinary responses keep the historical BLAS fast path. Pooled and Between
    legacy R-squared centering also reuse the existing range-safe working-scale
    policy when physical `y-mean(y)` subtraction would overflow.

16. **CRITICAL — RandomEffects common residual scaling could silently force a
    nonzero variance component to zero.**
    Swamy-Arora compares within and between auxiliary residual sums of squares on a
    common scale. If one residual series is much smaller, common normalization can
    erase a nonzero residual directly; even when the scaled residual survives, its
    square can underflow before RSS accumulation. The unsupported float64 range now
    fails closed with `FloatingPointError` instead of setting `sigma_e` or the
    corresponding variance contribution to a false exact zero.

17. **CRITICAL — RandomEffects quasi-demeaning could discard an identified
    within/between component.**
    The historical materialization `y-theta*y_bar` / `X-theta*X_bar` is certified
    against `within+(1-theta)*bar`. Exact nonzero-to-zero multiplication loss,
    complete loss of a smaller additive component, finite-to-nonfinite composition,
    or a globally material direct/decomposed disagreement fails closed. The
    positive square-root complement is retained before forming `theta=1-complement`
    so a representable complement is still visible to the certificate when the
    subtraction itself rounds `theta` to exactly one.

18. **MEDIUM — second-pass physical/documentation coverage.**
    NumPy and maintained Torch CPU regressions cover the automatic-intercept tail,
    rank-deficient minimum-norm preservation, RE common-normalization loss, RSS
    square underflow, quasi-demean component loss, and pre-rounded theta complement.
    `dev/benchmarks/validate_panel_intercept_cancellation_gpu.py` is the fresh
    CuPy/Torch CUDA gate for these paths, with exact-SHA/clean-tree/backend/GPU
    provenance. English and Chinese PooledOLS, BetweenOLS, and RandomEffects pages
    document the new numerical behavior and fail-closed limits.

## Rejected intermediate approaches

Strict regressions were not weakened to make implementation failures pass. Rejected
second-pass designs include an initial "stable mean -> observation-level centering ->
restore intercept" approach: re-review showed that subtraction can itself erase the
removed mean on huge rows and make the result backend-order dependent. The first
RandomEffects component certificate also used an elementwise near-zero relative
comparison and was rejected after the maintained near-collinear full-rank regression
showed it was too broad. The final certificate uses global transformed scale for the
roundoff comparison while retaining separate exact component-loss checks.

The only oracle adjustment was the Pooled/Between `1/3` intercept assertion from
bitwise equality to four float64 eps relative tolerance. This was required because
SVD basis vectors are themselves rounded; the observed discrepancy was one ulp
(`5.55e-17`), whereas the historical bug returned exactly zero. The statistical
reference value and all existing regression tolerances were otherwise unchanged.

## Validation

Implementation head `e4dcaeaf05af8b07f3859c09c49b1fe9382b1466` passed the maintained
`Tests` workflow, including the complete CPU tree, maintained Torch 2.0 regressions,
Python 3.9/3.10/3.11/3.12 matrix, static/docs contracts, and current `linearmodels`
alignment. The independent Panel Stage C Torch CPU workflow, maintenance compatibility,
release-note validation, and release-package validation also passed on that source.

The full CPU log contained no warning from the coefficient/variance/quasi-demeaning
implementation itself. The first extreme nonrobust RE test additionally exercised the
Hausman-only numerical fingerprint audit moments and emitted overflow warnings before
the intended RE fail-closed point; later precision tests and the physical validator
use `cov_type="hc0"` to isolate the Swamy-Arora/GLS path under review. The authoritative
Hausman identity remains the full-content SHA-256 digest; its audit-only raw moments
are outside this second-pass numerical change.

Historical inline review threads were 4/4 resolved and outdated before this final
synchronization. No temporary review workflow or patch-helper file is retained.

## Current exit condition

Fresh code review is clean with respect to locally actionable CRITICAL, HIGH, and
relevant MEDIUM findings. Final exact-head hosted CI must be rerun after this review
and documentation synchronization. Fresh physical GPU acceptance remains required on
both CuPy CUDA and Torch CUDA, including the new intercept/RandomEffects physical
gate, before merge readiness can be promoted. Historical P100 artifacts must not be
used as exact-head acceptance evidence.
