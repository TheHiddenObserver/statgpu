{
  "skill_name": "code-review",
  "evals": [
    {
      "id": 1,
      "prompt": "Review PR #136 in audit mode. I may be checked out on another branch.",
      "expected_output": "The review resolves PR #136 to immutable base/head SHAs instead of substituting the current checkout, reads dev/AGENTS.md, reports target identity, makes no edits, and rechecks that neither the PR base nor head moved before the verdict.",
      "assertions": [
        "The response records an exact base SHA and head SHA for PR #136.",
        "The response does not treat the current checkout as the PR target unless it proves they match.",
        "The response states or demonstrates that dev/AGENTS.md was used as project grounding.",
        "The final verdict is explicitly scoped to a re-resolved exact base/head pair."
      ]
    },
    {
      "id": 2,
      "prompt": "Review my current changes. No PR or branch scope specified; the working tree is intentionally dirty because I just implemented the feature.",
      "expected_output": "The review resolves the repository default branch, merge-base and current HEAD; treats the existing staged/unstaged/untracked state as the legitimate reviewed_before target; fingerprints that content rather than identifying it only by paths; and rechecks base/head/fingerprint freshness before the verdict.",
      "assertions": [
        "The response defines a base SHA and current head SHA.",
        "The response accounts for staged, unstaged, and relevant untracked changes.",
        "The response records a content fingerprint for the dirty working-tree target.",
        "The final freshness check recomputes the working-tree fingerprint."
      ]
    },
    {
      "id": 3,
      "prompt": "Auto-fix the issues in PR #136, but do not change branches or commit anything. My current checkout has unrelated uncommitted work.",
      "expected_output": "The skill resolves PR #136 first and refuses to edit the unrelated/pre-dirty checkout, reporting a target/write-state mismatch and recommending a clean PR-head worktree rather than layering fixes onto unrelated changes.",
      "assertions": [
        "The response never edits an unrelated or pre-dirty checkout for an explicit PR target.",
        "The response identifies the target/write-state mismatch.",
        "The response does not commit or push."
      ]
    },
    {
      "id": 4,
      "prompt": "Review only statgpu/linear_model for the changes on my branch.",
      "expected_output": "The skill treats the path as a filter on a branch/working-tree target, still resolves immutable base/head identity, and does not mistake the path itself for the comparison base.",
      "assertions": [
        "The response records the branch/working-tree base and head identity.",
        "The response applies statgpu/linear_model as a path filter."
      ]
    },
    {
      "id": 5,
      "prompt": "/code-review --fix PR #136 --comment; leave the fixes uncommitted and do not push.",
      "expected_output": "The project skill treats --fix as auto-fix and --comment as permission to publish a PR review summary, but it does not claim the remote PR is fixed/clean when the fixes exist only in a local dirty worktree. Any comment distinguishes the original remote-head verdict/findings from local draft fixes.",
      "assertions": [
        "--fix has the same mutation semantics as auto-fix.",
        "A comment never represents unpushed local fixes as part of the remote PR head.",
        "The response does not treat --comment as merge, push, or branch-mutation authorization."
      ]
    }
  ]
}
