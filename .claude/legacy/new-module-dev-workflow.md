# Legacy new-module-dev workflow note

The former path `.claude/workflows/new-module-dev.md` contained a Markdown-era repository workflow document.

Claude Code now uses `.claude/workflows/` as the active namespace for Dynamic Workflow scripts, so archival compatibility documentation no longer lives there.

Canonical current instructions:

- `.claude/skills/new-module-dev/SKILL.md`
- `.claude/skills/new-module-dev/workflow.md`

Historical records that mention the former path are intentionally left unchanged; the old file remains recoverable from git history.
